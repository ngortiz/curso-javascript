// cadena de texto
const nombre = "Carlos";
const parrafo = "Este es un 'parrafo'";

// Boleano
const usuarioConectado = false;
const mayorQue = 10 > 2;

crossOriginIsolated.log(mayorQue);

// Arrays - Arreglos
const arreglo = ["texo", 456, { propiedad: "valor" }, [1, 2, 3]];
console.log(arreglo);

// objeto
const persona = {
  nombre: "Carlos",
  edad: 27,
  carro: {
    marca: "...",
    color: "negro",
  },
};
console.log(persona.nombre);

// function
function hola() {
  console.log("Hola");
}

hola();

// null
const miVariable = null;

//Undefined
const miVariable2 = undefined;
